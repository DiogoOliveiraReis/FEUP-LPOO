package com.dor.tests;

/*
import com.badlogic.gdx.Input;
import com.dor.*;
import com.dor.controller.PlayerController;
import com.dor.pkmn.Player;
import com.dor.pkmn.TileMap;
import static org.junit.Assert.*;
*/


public class PlayerControllerTest
{
    /*
    TileMap mapa = new TileMap(5,5);
    Player p = new Player(mapa, 1,1);
    PlayerController pc = new PlayerController(p);

    @org.junit.Test
    public void PlayerMoveTest() {

        int keycode = Input.Keys.UP;

        pc.keyDown(keycode);
        assertEquals(2, p.getY());

        keycode = Input.Keys.DOWN;
        pc.keyDown(keycode);
        assertEquals(1, p.getY());

        keycode = Input.Keys.LEFT;
        pc.keyDown(keycode);
        assertEquals(0, p.getX());

        keycode = Input.Keys.RIGHT;
        pc.keyDown(keycode);
        assertEquals(1, p.getX());



    }

    @org.junit.Test
    public void getBrockDialogTest() {

        assertEquals(false, pc.getBrockDialog());

    }

    @org.junit.Test
    public void resetBrockDialog() {

        pc.resetBrockDialog();
        assertEquals(false, pc.getBrockDialog());

    }

    @org.junit.Test
    public void getNPC1DialogTest() {

        assertEquals(false, pc.getNPC1Dialog());
    }

    @org.junit.Test
    public void resetNPC1Dialog() {

        pc.resetNPC1Dialog();
        assertEquals(false, pc.getNPC1Dialog());
    }

    @org.junit.Test
    public void getPokemonBattle() {

        assertEquals(false, pc.getPokemonBattle());

    }

    @org.junit.Test
    public void resetPokemonBattle() {

        pc.resetPokemonBattle();
        assertEquals(false, pc.getPokemonBattle());

    }
    */
}